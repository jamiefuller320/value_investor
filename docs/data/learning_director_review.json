{"reviewed_at":"2026-09-20T10:57:25.806892+00:00","run_at":"2026-09-20T10:56:17.283372+00:00","enabled":true,"history_run_count":17,"sections":{"regime_assumption_check":"With `history_run_count` 17, `regime_summary.windows[full_sample]` still shows modest exclusion benefit (`cumulative_exclusion_alpha` 0.00261, `positive_alpha_rate` 0.5556, `bottom_quartile_exclude_rate` 0.1276, `top_quartile_retain_rate` 0.8794) and replay lift (`replay_return_delta_vs_actual` 0.057, `ladder_replay_readiness.ready_for_shadow_spawn` true), but the primary book does not validate the top-pick story (`primary_excess_after_costs` −0.3301, `beat_market` false, `beat_control` true, flag `primary_underperforming_market`). Rolling 8/16/24 slices are not yet in the payload (`regime_summary.windows` note points at phase `regime_slices_8_16_24`), so decay versus the static full-sample fit is unmeasured. Trajectory change dominates static fit: `trajectory_evidence.prediction_hit_rate_by_horizon` is weak at 1w (0.406, n=266) and 8w (0.3889, n=18), better at 4w (0.519, n=158), with 12w unscored (0 events); `upgrade_events` mean forward return −0.003031 (positive_rate 0.3469) while `downgrade_events` are mildly positive (0.006744), and opinion-flip keys `hold->buy` / `buy->strong_buy` at 1w show positive_rate 0.25 with negative means — flips are not paying off next week despite `weeks_to_realization.within_4w_rate` 0.9448 and `median_weeks` 1, which supports a timing/conviction lag (`model_focus_candidates` realization_lag) rather than abandoning exclusion alpha outright.","convergence":"`convergence_summary.strands` align on ai_judgment at the top (`live_primary.min_conviction` 0.6, `max_positions` 3, `applied` false, 15 epoch marks) and u4 at the bottom (`exclusion_ladder.step_id` u4, min_conviction 0.35), with calibration priors/shadows frozen at 0.0 versus live 0.6. `convergence_summary.tensions` block a bettable cohort: live conviction above ladder (`live_primary_min_conviction_above_ladder:0.6>0.35@u4`), priors not ready and directionally opposed, sleeve conflict (3 vs 5), negligible min_conviction in calibration, and `bet_gate_unmet:filtered_cohort_track_not_active` (`bet_gate_met` false). Strands are not converging yet — bottom filter replay is ready (`exclusion_ladder_replay.readiness.ready_for_shadow_spawn` true, shadow `ai_judgment_exclusion_u4` exists) but `cross_shard_phase_status` is `planned` and the bet gate is open. Follow `convergence_summary.look_now`: observe u4 shadow vs live, do not auto-apply priors, treat min_conviction as churn guard until `dual_objective_calibration` / `filtered_cohort_track`, and count forward weeks of shadow differentiation (≥4 per vision). `trajectory_evidence.model_focus_candidates` imply assessment-model gaps on upgrade transitions and 1w/8w directional sign, plus timing delay (1w miss vs fast realization). `hypothesis_integrity`: primary/rules/exclusion_u4 are within tolerance with empty `selection_feedback_flags`; `momentum_grace` is out of band (`loser_share` 0.6, `balancing_hint` trim_weakening_losers, all weakening theses) — thesis trim, not crude stops. `hypothesis_outcomes` on primary is not ready (`readiness.ready_for_thesis_outcome_analysis` false; hold_closed_with_thesis 7/8 target); no intact/broken recovery rates yet — observe-only. `entry_dca_overlay`: `lifecycle_coverage.perpetual` true, 27 factors / 18 observing, `ready_for_cadence_analysis` true, `leading_cadence` dca_4x_weekly, `model_independent_hint` true. `entry_dca_adoption`: acked, `current_stage` out_of_sample_first_entry — do not re-ack or execute DCA until `paper_execute_graduated`. `system_gaps.flags` (4 total, 2 high): highest-severity produce issue is `research_skipped_already_done` (executed 0, 451 already_researched) paired with `thin_memo_counted_as_coverage` (euro_depth 8/19 thin/zero-body; remedy ingest_then_body_lag_rememo — no rememo_reason widening while bodies flat). `analysis_review.sections.system_gaps` names all four flags; this is research production mistaken for learning-path application (written≠wired quality, existence≠body depth). FTSE apply layer looks wired (60/62 buy-tier) while produce/learning clocks diverge (e.g. sp500 `filing_ready_learning_stale`).","complexity_inventory":"`experiment_inventory.complexity`: within budget (`needs_agent_triage` false, `discretionary_open` 1 of `budget.max_discretionary_tasks` 12, `expensive_shadows` 4 of 8, `over_budget` false); `lifecycle_overlays.factor_coverage.perpetual` true with no stage gaps — do not retire the entry DCA overlay to free slots. Open discretionary work is thin: one `analysis_tasks` row (`ana-20260728-04`, monitoring, proposed). Four frozen shadows (three calibration + u4 exclusion) consume expensive slots but are not over cap. `experiment_assessment.summary`: continue 4, fail 5, recommend 1 (`entry_dca_overlay`, human_acked ack_observe); `recommendations` array empty — disposition lives in `by_status.recommend`. Failed assessment on u4/momentum_grace/calibration shadows is a signal to park losing expensive marks later (`experiment_lineage_and_park`), not to cancel lifecycle coverage. Analysis-review proposed two **[scoring]** trajectory experiments (hold→buy, family-weight counterfactual) in prose but did not queue analysis_task rows — only monitoring `ana-20260728-04` is open; top trajectory focus `hold->buy` still needs a canonical analysis task for human promote. Paper-learning-review did propose churn/monitoring/offline_sim items when exit_shadow stayed at closed_count 0 — aligned with triggers.","vision_roadmap_review":"- `v1_weekly_director` (active): **HOLD** — core orchestration; always on.\n- `regime_slices_8_16_24` (planned): **ACTIVATE** (proposal-only, human ack) — `history_run_count` 17 meets `revisit_when` (≥16); full_sample alone hides decay while trajectory flips misfire at 1w.\n- `buy_tier_level_book` (active): **HOLD** — epoch building; compare after ≥8 marks per phase note.\n- `buy_cross_archive_lab` (active): **HOLD** — Sunday observe-only; pairs with `ldr-20260823-03` archive replay gap.\n- `filtered_cohort_track` (planned): **HOLD** — u4 shadow exists but `bet_gate_met` false; need ≥4 weeks forward differentiation (`convergence_summary.look_now`) though u4 ladder alpha (0.000508) lags u3 (0.00261) in `exclusion_universe.ladder_results_slim`.\n- `loser_pattern_lab` (planned): **HOLD** — waits on `filtered_cohort_track` or run_count ≥20 (17 now).\n- `filter_invention_loop` (planned): **HOLD** — dependency chain not met.\n- `dual_objective_calibration` (planned): **HOLD** — needs filtered cohort marks.\n- `graduated_allocation_track` (active): **HOLD** — continue assessment (`gate_marks` 9, continue status); human ack on recommend deferred in analysis_review.\n- `capital_rotation_coordinator` (planned): **HOLD** — exit_timing / graduated gates not met.\n- `conviction_weighted_sizing` (planned): **HOLD** — downstream of graduated + rotation.\n- `cross_shard_winner_selection` (planned): **HOLD** — single-shard bet gate open.\n- `entry_lifecycle_experiments` (active): **HOLD** — overlay at cadence analysis; adoption staged, not execute.\n- `hypothesis_first_exit` (active): **HOLD** — thesis outcome linker not ready on primary.\n- `experiment_lineage_and_park` (planned): **HOLD** — `complexity.needs_agent_triage` false; revisit if triage fires 3 Sundays or failed shadows accumulate.\n- `super_director_auto_promote` (deferred): **RETIRE** from active consideration — explicit human ops only per `revisit_when`.","proposed_actions":"1. [analysis] Ensure scoring experiment for hold->buy — queue human-promotable offline replay on `trajectory_evidence.model_focus_candidates` hold->buy 1w (positive_rate 0.25, n=20) so analysis-review prose item #1 becomes a tracked task, not a parallel director loop.  \n2. [monitoring] Continue ai_judgment_exclusion_u4 forward observation — expected learning value: test whether u4 replay delta (`regime_summary.ladder_replay_readiness.primary_return_delta_vs_actual` 0.057) persists vs live primary (`ldr-20260823-02`).  \n3. [research] Thicken euro_depth filings then body-lag rememo — expected learning value: unblock learning consumer quality per `system_gaps.flags.thin_memo_counted_as_coverage` remedy (8 thin of 19; executed 0) without widening rememo_reason.  \n4. [monitoring] Exit-shadow cohort thickness — canonical task `ana-20260728-04` (proposed); expected learning value: unlock grace-vs-rotation learning when any track reaches ≥10 closed exits (currently 0 on focus tracks).  \n5. [offline_sim] Continue archive-lab full-period rebalance replay — `ldr-20260823-03`; expected learning value: reconcile signal backtest vs primary underperformance without live knob apply.","horizon_fragments":"- [trajectory,horizon,hypothesis] If `weeks_to_realization.within_4w_rate` 0.9448 but 1w hit_rate 0.406, monthly scan could treat “opinion change” as a 2–4 week forecast object instead of next-week screen delta — does the assessment model need an explicit horizon label per transition?  \n- [exclusion,optimization,hypothesis] u3 beats u4 on `exclusion_universe.ladder_results_slim` cumulative alpha (0.00261 vs 0.000508) while replay spawn recommends u4 — what if cohort-quality optimisation target u3 retain/exclude rates and portfolio replay target u4 are intentionally different objectives?","defer":"- Live decision-review `--apply`, primary knob promotion, and calibrated-shadow merges until `beat_market` true and `graduated_allocation_track` human ack on recommend completes (`dual_objective_calibration`, `filtered_cohort_track` bet gate).  \n- `filtered_cohort_track` activation and any auto-spawn of new exclusion shadows beyond observing `ai_judgment_exclusion_u4` until ≥4 forward weeks of shadow differentiation (`filtered_cohort_track`, `convergence_summary.look_now`).  \n- DCA execution on paper books or starter-fraction changes — `entry_dca_adoption` stays at `out_of_sample_first_entry`; `paper_execute_graduated` and `primary_or_live` remain blocked.  \n- `assign_signal` / live-screen mutation from transition keys until hold→buy scoring experiments show stable 4w+ lift (`filter_invention_loop` dependency chain).  \n- `momentum_grace` knob retune — defer to `hypothesis_first_exit` + exit-shadow thickness (`ana-20260728-04` gate); paper-learning already deferred.  \n- `loser_pattern_lab`, `filter_invention_loop`, `cross_shard_winner_selection` — remain manual until filtered cohort marks and multi-shard Phase 3 triggers in vision `revisit_when`.  \n- `super_director_auto_promote` — no revisit unless explicit human ops decision."},"fragments_compiled":{"compiled_at":"2026-09-20T10:57:25.804296+00:00","run_stamp":"20260920","created_count":2,"skipped_duplicates":[],"fragments":[{"id":"frag-20260920-01","text":"If `weeks_to_realization.within_4w_rate` 0.9448 but 1w hit_rate 0.406, monthly scan could treat “opinion change” as a 2–4 week forecast object instead of next-week screen delta — does the assessment model need an explicit horizon label per transition?","tags":["trajectory","horizon","hypothesis"],"status":"open","source":"learning_director:20260920","added_at":"2026-09-20T10:57:25+00:00"},{"id":"frag-20260920-02","text":"u3 beats u4 on `exclusion_universe.ladder_results_slim` cumulative alpha (0.00261 vs 0.000508) while replay spawn recommends u4 — what if cohort-quality optimisation target u3 retain/exclude rates and portfolio replay target u4 are intentionally different objectives?","tags":["exclusion","optimization","hypothesis"],"status":"open","source":"learning_director:20260920","added_at":"2026-09-20T10:57:25+00:00"}]}}