{"reviewed_at":"2026-09-06T13:53:11.824381+00:00","run_at":"2026-09-06T13:52:27.587235+00:00","enabled":true,"history_run_count":13,"sections":{"regime_assumption_check":"On the sole active window (`regime_summary.windows[0]`, full_sample, 13 runs / 9 exclusion week-pairs), bottom-filter priors still hold at archive step u3: `cumulative_exclusion_alpha` 0.00261, `positive_alpha_rate` 0.5556, `bottom_quartile_exclude_rate` 0.1276, `top_quartile_retain_rate` 0.8794 — but tightening is non-monotonic (`exclusion_universe.ladder_results_slim`: u4 alpha 0.000508, u5 −0.005175, u5 `mean_bottom_quartile_exclude_rate` 0.8924). Primary-track market assumption remains broken (`primary_excess_after_costs` −21.47%, `beat_market` false, `beat_control` true; flag `regime_summary.flags[0]` `primary_underperforming_market`). Rolling 8/16/24 slices are still inactive (`regime_summary.windows[0].note`; `history_run_count` 13 < 16 for `regime_slices_8_16_24`). Trajectory change dominates static archive fit: `trajectory_evidence.prediction_hit_rate_by_horizon` 1w 0.406 (n=266), 4w 0.519 (n=158), 8w 0.3889 (n=18), 12w unscored; upgrades underperform downgrades (`upgrade_events.positive_rate` 0.3469 vs `downgrade_events.positive_rate` 0.5238). Realization lag is the leading timing misfire, not signal absence: `weeks_to_realization.median_weeks` 1, `within_4w_rate` 0.9448, `realized_within_12w_rate` 0.5993 (n=181 realized / 302 scorable) — opinion flips fire before 1w price confirms.","convergence":"Bottom-filter and top-pick strands agree on mechanism but not on a deployable cohort. `convergence_summary.strands.exclusion_ladder` and `exclusion_shadow` target u4 (`step_id` u4, `min_conviction` 0.35, track `ai_judgment_exclusion_u4`) while archive priors favour u3 (`regime_summary.recommended_exclusion_step` u3; `exclusion_universe.recommended_step.cumulative_exclusion_alpha` 0.00261); live primary runs tighter still (`strands.live_primary.min_conviction` 0.6, `max_positions` 3, `epoch_equity_marks` 6, `applied` false). Tensions are explicit in `convergence_summary.tensions`: `live_primary_min_conviction_above_ladder:0.6>0.35@u4`, `calibration_priors_direction_opposes_live:0.0_vs_0.6_not_ready`, `sleeve_count_conflict:live_3.0_priors_5.0`, `bet_gate_unmet:filtered_cohort_track_not_active`. Replay supports filter value (`regime_summary.ladder_replay_readiness.primary_return_delta_vs_actual` 0.057, `ready_for_shadow_spawn` true) yet `convergence_summary.bet_gate_met` is false — strands are not converging toward a bettable filtered cohort this week. Per `vision.prediction_philosophy`, success is timely opinion updates: `convergence_summary.look_now` directs forward observation on `ai_judgment_exclusion_u4` vs live primary, no auto-apply of knob priors while calibration conflicts persist, treat `min_conviction` as churn guard until dual-objective signal exists, and count consecutive shadow-differentiation weeks toward `filtered_cohort_track` (≥4). `trajectory_evidence.model_focus_candidates` imply an assessment-model timing/conviction-delay gap (top: `hold->buy` 1w positive_rate 0.25 n=20; `realization_lag` median 1w / within_4w 0.9448) — not a live-screen mutation target. `hypothesis_integrity.tracks.*` all `within_tolerance` true; no `selection_feedback_flags` on any track (momentum_grace 1 intact loser, `balancing_hint` `tolerate_intact_losers` — expected, not a crude-stop trigger). `hypothesis_outcomes.tracks.*.readiness.ready_for_thesis_outcome_analysis` false everywhere (0 `hold_closed_with_thesis`) — defer thesis-outcome learning. `entry_dca_overlay.lifecycle_coverage.perpetual` true (27 factors, 18 observing, 0 `stages_without_observing_experiment`); `ready_for_cadence_analysis` false, `scored_count` 0, `model_independent_hint` false — overlay is the experiment; do not spawn a new paper book. `system_gaps.flags[0]` `observe_clock_stale` (medium, sp500 `screen_stale` true, last_screen 2026-08-16 despite 12 observe_snapshots) — `analysis_review.sections.system_gaps` correctly names it; this is filing-ready≠learning_ready (`filing_ready` false, `learning_ready` false on sp500), not a weekly_ops budget miss (`healthy_counter_distrust.filing_parity_is_not_learning_ready` true).","complexity_inventory":"`experiment_inventory.complexity` is within budget: `discretionary_open` 1 / `budget.max_discretionary_tasks` 12, `expensive_shadows` 4 / `max_expensive_shadow_tracks` 8, `coverage_perpetual` true, `coverage_gap` false, `needs_agent_triage` false — no parking required; keep lifecycle overlay marks running. `lifecycle_overlays.factor_coverage.perpetual` true (27 factors across all stages) — do not retire observing stages to free slots. `experiment_assessment.summary`: 0 proposed, 0 recommend, 3 continue, 4 fail, 2 observing; `recommendations` empty (human ack none pending). Fail rows: three calibration shadows (`gate_excess_after_costs` −6.25% to −10.94%, 10–13 marks) and `momentum_grace` (−2.55%, 21 marks) — losing discretionary candidates if triage fires later, not the lifecycle overlay. Observing: `ai_judgment_exclusion_u4` (`gate_marks` 0) and `entry_dca_overlay` (0 scored). Open discretionary slot is `ana-20260728-04` (monitoring, exit-shadow thickness). Analysis-review fired scoring triggers (`analysis_review.sections.proposed_experiments` items 1–2: trajectory timing delay + quality-family gate) but no canonical `analysis_tasks` id exists for either — only monitoring task queued. Paper-learning-review also proposed three churn/offline items (`paper_learning_review.sections.proposed_experiments`) with 0 `paper_learning_tasks` open; proposals exist in review prose, not tracked tasks.","vision_roadmap_review":"**regime_slices_8_16_24** — HOLD. `revisit_when` requires `history_run_count` ≥16; currently 13. Rolling decay/reversal detection cannot activate yet.\n\n**filtered_cohort_track** — HOLD (proposal-only). `exclusion_ladder_replay.readiness.ready_for_shadow_spawn` true and u4 alpha barely positive (0.000508), shadow exists (`ai_judgment_exclusion_u4`, `gate_marks` 0) — but ≥4 consecutive forward-differentiation weeks not met (`convergence_summary.look_now[3]`).\n\n**loser_pattern_lab** — HOLD. Depends on `filtered_cohort_track` active or `history_run_count` ≥20; neither gate met.\n\n**filter_invention_loop** — HOLD. Depends on loser_pattern_lab ≥3 validated patterns; not started.\n\n**dual_objective_calibration** — HOLD. Requires `filtered_cohort_track` ≥8 epoch marks; cohort track not active.\n\n**capital_rotation_coordinator** — HOLD. `exit_timing_cohorts.readiness` empty (0 live closed hold/swap); `graduated_allocation_track` rebalance_log immature.\n\n**conviction_weighted_sizing** — HOLD. Depends on graduated_allocation ≥8 marks and stable cost_drag; prerequisites unmet.\n\n**cross_shard_winner_selection** — HOLD. Requires ≥2 shards at Phase 3 plus FTSE `filtered_cohort_track` ≥8 marks; neither met.\n\n**experiment_lineage_and_park** — HOLD. `complexity.needs_agent_triage` false; three failed calibration shadows still cheap weekday marks — park logic premature.\n\n**super_director_auto_promote** (deferred) — RETIRE from active consideration. `revisit_when` is explicit human ops decision only; guardrails `no_auto_engineering_promote` remain in force.\n\nActive phases (`v1_weekly_director`, `graduated_allocation_track`, `entry_lifecycle_experiments`, `hypothesis_first_exit`) — continue observe-only; no activation proposals this week.","proposed_actions":"1. [monitoring] Continue forward pass on `ai_judgment_exclusion_u4` vs live primary (`ldr-20260823-02`; aligns `convergence_summary.look_now[0]`) — expected learning value: first cost-aware week-pair read on whether u4 ladder replay alpha (`exclusion_ladder_replay.readiness.primary_return_delta_vs_actual` 0.057) converts to post-cost shadow differentiation; counts toward `filtered_cohort_track` ≥4-week gate.\n\n2. [analysis] Ensure scoring experiment for `hold->buy` transition key — expected learning value: convert `analysis_review.sections.proposed_experiments` item 1 from review prose into a tracked `analysis_tasks` entry; offline 1w vs 4w confirmation gate on n=336 labeled events where 1w `prediction_hit_rate` 0.406 and `weeks_to_realization.within_4w_rate` 0.9448 imply premature upgrades (`trajectory_evidence.model_focus_candidates[0]`).\n\n3. [analysis] Ensure scoring experiment for quality-family composite gate — expected learning value: convert `analysis_review.sections.proposed_experiments` item 2 into tracked task; tests false-upgrade rate where quality failed in 9/53 `loser_snapshot_cards.top_failed_families` (avoid-tier AAL.L/AML.L pattern at conviction ≥0.35).\n\n4. [monitoring] Continue exit-shadow cohort thickness dashboard (`ana-20260728-04`; `experiment_assessment.by_status.continue[0]`) — expected learning value: alert when any track reaches ≥10 closed exits with grace vs screen_rotation split; prerequisite before momentum_grace knob experiments (`exit_shadow` closed_total 0 across focus tracks).\n\n5. [offline_sim] Continue archive-lab full-period rebalance replay (`ldr-20260823-03`) — expected learning value: reconcile constructive signal backtests (+2.99% 28d buy, `analysis_review.sections.signal_backtest_findings`) with −21.47% primary excess; isolate selection/churn vs rank before any knob apply.","horizon_fragments":"- [trajectory,downgrade_alpha,hypothesis] Downgrades outperform upgrades at 1w (`downgrade_events.positive_rate` 0.5238 vs `upgrade_events.positive_rate` 0.3469) — what if the assessment model optimised for timely exit/downgrade calls rather than entry upgrades, and entry sizing deferred until 4w realization confirms?\n\n- [exclusion_ladder,regime_indicator,hypothesis] u3→u4→u5 alpha cliff (0.00261 → 0.000508 → −0.005175) may be a live regime-stress indicator rather than a static step picker — could weekly director flag \"tighten/loosen ladder\" from rolling exclude-rate velocity instead of cumulative alpha alone?","defer":"- **filtered_cohort_track** — await ≥4 consecutive weeks of `ai_judgment_exclusion_u4` forward differentiation (`convergence_summary.look_now[3]`; currently `gate_marks` 0).\n- **regime_slices_8_16_24** — `history_run_count` 13 < 16; rolling decay detection inactive.\n- **dual_objective_calibration**, **loser_pattern_lab**, **filter_invention_loop**, **capital_rotation_coordinator**, **conviction_weighted_sizing**, **cross_shard_winner_selection** — dependency chains unmet; no human ack for builds.\n- **entry_dca_overlay** cadence ranking — `readiness.closed_episodes` 0 / target 12; overlay remains collect-only (`entry_dca_overlay.readiness.ready_for_cadence_analysis` false).\n- **hypothesis_first_exit** thesis-outcome analysis — all tracks `hold_closed_with_thesis` 0 (target ≥8); prefer thesis review over crude stops until linker matures.\n- Knob prior live apply (`max_positions` 5, `min_conviction` 0.0) — `regime_summary.knob_calibration_ready` false; three calibration shadows failed; `convergence_summary.tensions` include direction conflicts.\n- Auto-spawn exclusion shadows beyond existing u4 book, decision-review `--apply`, and live screen mutation from transition keys — human gate only per `guardrails`."},"fragments_compiled":{"compiled_at":"2026-09-06T13:53:11.822379+00:00","run_stamp":"20260906","created_count":2,"skipped_duplicates":[],"fragments":[{"id":"frag-20260906-01","text":"Downgrades outperform upgrades at 1w (`downgrade_events.positive_rate` 0.5238 vs `upgrade_events.positive_rate` 0.3469) — what if the assessment model optimised for timely exit/downgrade calls rather than entry upgrades, and entry sizing deferred until 4w realization confirms?","tags":["trajectory","downgrade_alpha","hypothesis"],"status":"open","source":"learning_director:20260906","added_at":"2026-09-06T13:53:11+00:00"},{"id":"frag-20260906-02","text":"u3→u4→u5 alpha cliff (0.00261 → 0.000508 → −0.005175) may be a live regime-stress indicator rather than a static step picker — could weekly director flag \"tighten/loosen ladder\" from rolling exclude-rate velocity instead of cumulative alpha alone?","tags":["exclusion_ladder","regime_indicator","hypothesis"],"status":"open","source":"learning_director:20260906","added_at":"2026-09-06T13:53:11+00:00"}]}}