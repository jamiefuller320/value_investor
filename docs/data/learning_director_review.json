{"reviewed_at":"2026-09-01T10:12:57.735314+00:00","run_at":"2026-09-01T10:12:25.878049+00:00","enabled":true,"history_run_count":11,"sections":{"regime_assumption_check":"Exclusion alpha and cohort floor metrics still hold on the full sample (`regime_summary.windows[0]`: `cumulative_exclusion_alpha` 0.00261, `positive_alpha_rate` 0.5556 over 9 week-pairs; `bottom_quartile_exclude_rate` 0.1276, `top_quartile_retain_rate` 0.8794), with archive priors at u3 (`regime_summary.recommended_exclusion_step` u3; `exclusion_universe.recommended_step.cumulative_exclusion_alpha` 0.00261). Tightening shows clear decay: `exclusion_universe.ladder_results_slim` u4 alpha 0.000508 and u5 −0.005175 with `mean_bottom_quartile_exclude_rate` 0.8924 — the bettable floor is step-sensitive, not monotonic. Primary-track market assumption still fails (`regime_summary.windows[0].primary_excess_after_costs` −19.24%, `beat_market` false, `beat_control` true; flag `regime_summary.flags[0]` `primary_underperforming_market`). Trajectory change dominates static archive fit: `trajectory_evidence.prediction_hit_rate_by_horizon` 1w 0.406 (n=266), 4w 0.519 (n=158), 8w 0.3889 (n=18); upgrades inverted vs downgrades (`upgrade_events.positive_rate` 0.3469 vs `downgrade_events.positive_rate` 0.5238). Realization lag suggests timing misfire not signal absence: `weeks_to_realization.median_weeks` 1, `within_4w_rate` 0.9448 — opinion flips fire early relative to 1w price. Rolling 8/16/24 slices remain inactive (`regime_summary.windows[0].note`; `history_run_count` 11 < 16).","convergence":"Bottom-filter and top-pick strands agree on mechanism but not on a deployable cohort. `convergence_summary.strands.exclusion_ladder` and `exclusion_shadow` (`ai_judgment_exclusion_u4`, `min_conviction` 0.35) target u4, while archive priors favour u3 (`exclusion_universe.recommended_step.step_id` u3); live primary runs tighter still (`strands.live_primary.min_conviction` 0.6, `max_positions` 3, `epoch_equity_marks` 3, `applied` false). Tensions are explicit in `convergence_summary.tensions`: `live_primary_min_conviction_above_ladder:0.6>0.35@u4`, `calibration_priors_direction_opposes_live:0.0_vs_0.6_not_ready`, `sleeve_count_conflict:live_3.0_priors_5.0`, `bet_gate_unmet:filtered_cohort_track_not_active`. Replay supports filter value (`regime_summary.ladder_replay_readiness.primary_return_delta_vs_actual` 0.057, `ready_for_shadow_spawn` true) yet `convergence_summary.bet_gate_met` is false — strands are not converging toward a bettable filtered cohort this week. Per `vision.prediction_philosophy`, success is observing timely opinion updates: `convergence_summary.look_now` directs forward pass on `ai_judgment_exclusion_u4` vs live primary, no auto-apply of `knob_calibration_priors`, treat `min_conviction` as churn guard until dual-objective signal exists, and count consecutive shadow-differentiation weeks toward `filtered_cohort_track` (≥4). `trajectory_evidence.model_focus_candidates` imply assessment-model gaps on conviction/timing calibration: `hold->buy` (1w `positive_rate` 0.25, n=20), `buy->strong_buy` (0.25, n=12), `signal_unchanged` (0.3431, n=239), plus aggregate 1w/8w directional miss and `realization_lag` candidate (1w hit 0.406 vs `within_4w_rate` 0.9448). `loser_snapshot_cards` (53 cards; quality 9, dividend 8, garp 6 in `top_failed_families`) reinforce filter weak spots via avoid-tier false-upgrade triggers on AAL.L, AML.L, etc. (`opinion_flip_triggers` \"passes quality family (currently failed)\"). `hypothesis_integrity` and `hypothesis_outcomes` are null — no thesis-outcome read; defer intact-loser tolerance until `ready_for_thesis_outcome_analysis` appears.","complexity_inventory":"Open load exceeds budget: `experiment_inventory.open_experiment_count` 8 vs `vision.complexity_budget.max_parallel_open_experiments` 5 (4 `analysis_tasks`, 3 `paper_learning_tasks`, 1 `horizon_tasks`; 0 `learning_director_tasks`). Shadow tracks at ceiling: `experiment_inventory.shadow_track_count` 4 equals `max_frozen_shadow_tracks` 4 — three calibration shadows (`ai_judgment_calibrated`, `_r2`, `_r3` at `min_conviction` 0.0) plus one exclusion shadow (`ai_judgment_exclusion_u4`). `experiment_assessment.summary`: 4 recommend (`human_ack_pending` 4), 4 fail, 2 continue, 3 observing. Fail status on all three calibration shadows (`experiment_assessment.by_status.fail`: `ai_judgment_calibrated` −9.59% excess, `_r2`/`_r3` ~−5.2%) and `momentum_grace` (−2.19%) — recommend **retire** consideration for rank-1 calibration shadow; **keep observing** u4 exclusion shadow only; **defer** `ana-20260728-02` (`paper_knobs` counterfactual) until primary epoch reaches ≥2 marks (`learning_tracks_review.reviews.ai_judgment.metrics.epoch.equity_marks` 3 but `post_apply_trade_count` 0, overlaps `hor-20260811-02` epoch-isolation hold). Recommend rows pending human ack only: `ana-20260728-04`, `ldr-20260823-01`, `ldr-20260823-02`, `ldr-20260823-04`. Specialist review coverage: `paper_learning_review` correctly queued all three churn proposals (`plr-20260901-01` through `-03`). **Gap:** `analysis_review.sections.proposed_experiments` items 1–2 (`[scoring]` conviction-timing overlay for `hold->buy`/`signal_unchanged`; quality-family gate) are **not** in `experiment_inventory.open_experiments.analysis_tasks` — only `ana-20260728-05` (overlay forward bootstrap) is open; scoring hypotheses exist in review prose, not the tracked queue.","vision_roadmap_review":"**v1_weekly_director** (active): **HOLD** — core orchestration; `revisit_when` always on.\n\n**regime_slices_8_16_24** (planned): **HOLD** — `history_run_count` 11 < 16 and `regime_summary.windows[0].exclusion_week_pairs` 9 < 12 consecutive pairs; u3/u4 alpha divergence strengthens case once triggered.\n\n**filtered_cohort_track** (planned): **HOLD** (proposal-only observe) — `exclusion_ladder_replay.readiness.ready_for_shadow_spawn` true and u4 shadow exists (`experiment_inventory.shadow_tracks[3]`), but u4 archive alpha thin (`exclusion_universe.ladder_results_slim` u4: 0.000508) vs u3 (0.00261), and ≥4 consecutive weeks of shadow forward differentiation not met per `revisit_when`.\n\n**loser_pattern_lab** (planned): **HOLD** — blocked by `regime_slices_8_16_24` and `history_run_count` 11 < 20.\n\n**filter_invention_loop** (planned): **HOLD** — requires ≥3 validated patterns from loser_pattern_lab; prerequisite inactive.\n\n**dual_objective_calibration** (planned): **HOLD** — requires `filtered_cohort_track` ≥8 epoch marks; cohort track not active.\n\n**graduated_allocation_track** (active): **HOLD** — `learning_tracks_review.reviews.graduated_allocation.metrics.equity_marks` 2, 4 trades; `revisit_when` needs ≥8 weekly marks.\n\n**capital_rotation_coordinator** (planned): **HOLD** — `exit_timing_cohorts` null; near-miss archive ready (`exit_timing_near_miss.readiness.ready_for_probability_analysis` true, 202 hold / 209 swap closed) but live closed swap rotations below gate; `graduated_allocation` rebalance_log too thin.\n\n**conviction_weighted_sizing** (planned): **HOLD** — upstream dependencies unmet.\n\n**cross_shard_winner_selection** (planned): **HOLD** — `convergence_summary.cross_shard_phase_status` planned; no second shard at Phase 3.\n\n**hypothesis_first_exit** (active): **HOLD** — phase active but `hypothesis_outcomes` absent; observe via `exit_timing_near_miss` hold recovery 73.27% in `pre_buy_0.28_0.45` band until thesis linker populates.\n\n**super_director_auto_promote** (deferred): **RETIRE** (continue deferring) — `revisit_when` explicit human ops decision; `guardrails.human_promotion_required` true with primary underperforming market.","proposed_actions":"1. [monitoring] Human-ack forward pass on ai_judgment_exclusion_u4 vs live primary (`ldr-20260823-02`; aligns `convergence_summary.look_now[0]`) — expected learning value: first cost-aware week-pair read on whether u4 ladder replay alpha (`exclusion_ladder_replay.readiness.primary_return_delta_vs_actual` 0.057) converts to post-cost shadow differentiation; counts toward `filtered_cohort_track` ≥4-week gate.\n\n2. [analysis] Ensure scoring experiment queued for hold->buy and signal_unchanged transition keys — expected learning value: convert `analysis_review.sections.proposed_experiments` item 1 (offline conviction-timing overlay; 1w `prediction_hit_rate` 0.406, n=266) from review prose into a tracked `analysis_tasks` entry; top trajectory focus per `trajectory_evidence.model_focus_candidates[0]` and `[2]`.\n\n3. [analysis] Ensure scoring experiment queued for quality-family composite gate on avoid cohort — expected learning value: convert `analysis_review.sections.proposed_experiments` item 2 into tracked task; tests false-upgrade rate where quality failed in 9/53 `loser_snapshot_cards` (AAL.L, AML.L pattern).\n\n4. [monitoring] Human-ack exit-shadow cohort thickness dashboard (`ana-20260728-04`; `experiment_assessment.recommendations[0]`) — expected learning value: alert when any track reaches ≥10 closed exits with grace vs screen_rotation split; prerequisite before momentum_grace knob experiments (`exit_shadow` closed_total 0 across focus tracks).\n\n5. [paper_churn] Human-ack extended buffered-hold counterfactual (`plr-20260901-02`) — expected learning value: 7d replay tied at delta 0 on rules/ai_judgment; extended lookback to epoch start 2026-08-12/26 may surface whether `exit_confirm_screens` 1 vs 2 changes trade count when exits actually fire.","horizon_fragments":"- [upgrade_inversion,trajectory_evidence,hypothesis] Upgrade events show 34.69% positive_rate vs 52.38% on downgrades (`trajectory_evidence.upgrade_events` / `downgrade_events`) — what if the assessment model is structurally better at timing exits than entries, and winner-picking should defer allocation until loser-filter cohort floor is proven?\n\n- [quality_veto_timing,loser_cards,hypothesis] Nine of 53 loser cards fail quality family while `opinion_flip_triggers` still permit conviction ≥0.35 upgrades above avoid — what if family-failure veto runs as a pre-upgrade gate rather than post-hoc forensics in loser_pattern_lab?","defer":"- **filtered_cohort_track** (`vision.phases[2]`): equal-weight cohort paper deployment — wait until u4 shadow accumulates ≥4 consecutive forward week-pairs with differentiation and human ack; shadow exists (`ai_judgment_exclusion_u4`) but forward history insufficient and u4 archive alpha thin (0.000508).\n\n- **regime_slices_8_16_24** (`vision.phases[1]`): rolling 8/16/24 decay panels — wait until `history_run_count` ≥16 and exclusion archive ≥12 consecutive week pairs (currently 11 runs, 9 pairs).\n\n- **loser_pattern_lab / filter_invention_loop / dual_objective_calibration** (`vision.phases[3–5]`): blocked by upstream phases and insufficient history.\n\n- Auto-apply `knob_calibration_priors` (`regime_summary.knob_calibration_ready` false; priors recommend `min_conviction` 0.0 / `max_positions` 5 with low confidence) — conflicts with live 0.6 floor and `convergence_summary.tensions` calibration direction conflicts.\n\n- Live assign_signal / base-screen mutation from trajectory transition keys — defer until offline scoring experiment confirms 4w hit_rate lift above 0.519 (`analysis_review.sections.defer`; `trajectory_evidence.prediction_hit_rate_by_horizon.4w` 0.519, n=158).\n\n- Auto-spawn exclusion or knob shadows — u4 exclusion shadow already observing; no additional spawn until human ack and forward differentiation documented.\n\n- **capital_rotation_coordinator / conviction_weighted_sizing / cross_shard_winner_selection**: dependencies on graduated_allocation epoch marks (2 lifetime), exit-timing live cohorts, and second-shard Phase 3 not met.\n\n- **hypothesis_first_exit thesis rotation** — defer until `hypothesis_outcomes.readiness.ready_for_thesis_outcome_analysis` true in a future payload (`hypothesis_integrity` currently null).\n\n- **super_director_auto_promote** (`vision.phases[11]`): explicit human ops decision only; no auto-promote to engineering."},"fragments_compiled":{"compiled_at":"2026-09-01T10:12:57.733960+00:00","run_stamp":"20260901","created_count":2,"skipped_duplicates":[],"fragments":[{"id":"frag-20260901-01","text":"Upgrade events show 34.69% positive_rate vs 52.38% on downgrades (`trajectory_evidence.upgrade_events` / `downgrade_events`) — what if the assessment model is structurally better at timing exits than entries, and winner-picking should defer allocation until loser-filter cohort floor is proven?","tags":["upgrade_inversion","trajectory_evidence","hypothesis"],"status":"open","source":"learning_director:20260901","added_at":"2026-09-01T10:12:57+00:00"},{"id":"frag-20260901-02","text":"Nine of 53 loser cards fail quality family while `opinion_flip_triggers` still permit conviction ≥0.35 upgrades above avoid — what if family-failure veto runs as a pre-upgrade gate rather than post-hoc forensics in loser_pattern_lab?","tags":["quality_veto_timing","loser_cards","hypothesis"],"status":"open","source":"learning_director:20260901","added_at":"2026-09-01T10:12:57+00:00"}]}}