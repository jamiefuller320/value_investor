{"reviewed_at":"2026-08-30T15:08:56.607317+00:00","run_at":"2026-08-30T15:08:36.322375+00:00","enabled":true,"history_run_count":11,"sections":{"regime_assumption_check":"Exclusion alpha still registers positive at the archive-recommended step u3 (`exclusion_universe.recommended_step.cumulative_exclusion_alpha` 0.00261, `positive_alpha_rate` 0.5556 over 9 week-pairs), with cohort floor metrics holding (`regime_summary.windows[0].bottom_quartile_exclude_rate` 0.1276, `top_quartile_retain_rate` 0.8794). However, tightening to u4/u5 shows clear decay/reversal (`exclusion_universe.ladder_results_slim[u4].cumulative_exclusion_alpha` 0.000508; u5 −0.005175 with `mean_bottom_quartile_exclude_rate` 0.8924), and the prior week's u4-first read has partially reversed — `regime_summary.recommended_exclusion_step` is now u3 while `exclusion_ladder_replay.readiness.recommended_step_id` remains u4 with replay lift (`primary_return_delta_vs_actual` 0.057). Primary-track market assumption still fails: `regime_summary.windows[0].primary_excess_after_costs` −19.24%, `beat_market` false, `beat_control` true — flag `regime_summary.flags[0]` `primary_underperforming_market`. Trajectory evidence is the dominant weak spot: `trajectory_evidence.prediction_hit_rate_by_horizon.1w.prediction_hit_rate` 0.406 (n=266), 4w 0.519 (n=158), 8w 0.3889 (n=18); upgrades show inverted sign (`upgrade_events.positive_rate` 0.3469 vs `downgrade_events.positive_rate` 0.5238). Realization lag suggests timing not absence of signal: `weeks_to_realization.median_weeks` 1, `within_4w_rate` 0.9448 — opinion flips fire early relative to 1w price, so static archive tier monotonicity (+2.03% buy 7d in `analysis_review.sections.signal_backtest_findings`) must not override forward trajectory calibration.","convergence":"Bottom-filter and top-pick strands are aligned on mechanism but not on a bettable cohort. `convergence_summary.strands.exclusion_ladder` and `exclusion_shadow` (`ai_judgment_exclusion_u4`, `min_conviction` 0.35) point u4, while archive priors favour u3 (`exclusion_universe.recommended_step.step_id` u3); live primary runs tighter still (`strands.live_primary.min_conviction` 0.6, `max_positions` 3, `epoch_equity_marks` 1). Tensions are explicit: `convergence_summary.tensions` includes `live_primary_min_conviction_above_ladder:0.6>0.35@u4`, `calibration_priors_direction_opposes_live:0.0_vs_0.6_not_ready`, `bet_gate_unmet:filtered_cohort_track_not_active`. Replay counterfactuals support filter value (`regime_summary.ladder_replay_readiness.primary_return_delta_vs_actual` 0.057, `ready_for_shadow_spawn` true), yet `convergence_summary.bet_gate_met` is false and no filtered-cohort paper track exists. Per `vision.prediction_philosophy`, success this week is observing exclusion-shadow forward marks and the fresh primary epoch — not reconciling archive backstory with −19.24% paper excess. `convergence_summary.look_now` directs: observe `ai_judgment_exclusion_u4` vs live primary, do not auto-apply `knob_calibration_priors`, treat `min_conviction` as churn guard until dual-objective signal exists, count consecutive shadow-differentiation weeks toward `filtered_cohort_track` activation (≥4). `trajectory_evidence.model_focus_candidates` imply assessment-model gaps on conviction/timing calibration: `hold->buy` (1w `positive_rate` 0.25, n=20), `buy->strong_buy` (0.25, n=12), `signal_unchanged` (0.3431, n=239), plus aggregate 1w/8w directional miss — not a universe-size problem. `loser_snapshot_cards` (53 cards; quality 9, dividend 8, garp 6 in `top_failed_families`) reinforce filter weak spots via avoid-tier false-upgrade triggers (`opinion_flip_triggers` \"passes quality family (currently failed)\" on AAL.L, AML.L, etc.). `hypothesis_integrity` and `hypothesis_outcomes` are null — no thesis-outcome read this week; defer intact-loser tolerance review until `ready_for_thesis_outcome_analysis` appears.","complexity_inventory":"Open load is at budget: `experiment_inventory.open_experiment_count` 5 equals `vision.complexity_budget.max_parallel_open_experiments` 5 (4 `analysis_tasks` + 1 `horizon_tasks`; 0 `paper_learning_tasks`). Shadow tracks are at ceiling: `experiment_inventory.shadow_track_count` 4 equals `max_frozen_shadow_tracks` 4 — three calibration shadows (`ai_judgment_calibrated`, `_r2`, `_r3` at `min_conviction` 0.0) plus one exclusion shadow (`ai_judgment_exclusion_u4`). `experiment_assessment.summary` shows 4 fail (all three calibration shadows + `momentum_grace`), 2 continue (`ana-20260728-01`, `ldr-20260823-03`), 4 recommend pending human ack (`ana-20260728-04`, `ldr-20260823-01`, `ldr-20260823-02`, `ldr-20260823-04`). Recommend deferring `ana-20260728-02` (`paper_knobs` counterfactual) until primary epoch reaches ≥2 marks (`learning_tracks_review.reviews.ai_judgment.metrics.epoch.equity_marks` 1, `benchmark_span_available` false) — overlaps with `hor-20260811-02` epoch-isolation hold. Do not spawn additional shadows; retire consideration for failed calibration rank-1 shadow (`experiment_assessment.by_status.fail` `ai_judgment_calibrated`, −7.45% excess) while keeping u4 exclusion shadow observing. Analysis-review **did** propose `[scoring]` experiments for top trajectory keys (`analysis_review.sections.proposed_experiments` items 1–2: conviction-timing overlay for `hold->buy`/`signal_unchanged`; quality-family gate) — but these are **not** queued in `experiment_inventory.open_experiments.analysis_tasks`; only `ana-20260728-05` (overlay forward bootstrap) is open. Gap: scoring hypotheses exist in review prose, not in the tracked experiment queue.","vision_roadmap_review":"**v1_weekly_director** (active): **HOLD** — core orchestration; `revisit_when` always on.\n\n**regime_slices_8_16_24** (planned): **HOLD** — `history_run_count` 11 < 16 and `regime_summary.windows[0].exclusion_week_pairs` 9 < 12 consecutive pairs per `revisit_when`; u3/u4 alpha divergence this week strengthens the case for rolling slices once triggered.\n\n**filtered_cohort_track** (planned): **HOLD** (proposal-only observe) — `exclusion_ladder_replay.readiness.ready_for_shadow_spawn` true and u4 shadow exists (`experiment_inventory.shadow_tracks[3]`), but u4 archive alpha is thin (0.000508) vs u3 (0.00261), and ≥4 consecutive weeks of shadow forward differentiation not yet met per `revisit_when`.\n\n**loser_pattern_lab** (planned): **HOLD** — blocked by `regime_slices_8_16_24` and `history_run_count` 11 < 20.\n\n**filter_invention_loop** (planned): **HOLD** — requires ≥3 validated patterns from loser_pattern_lab; prerequisite inactive.\n\n**dual_objective_calibration** (planned): **HOLD** — requires `filtered_cohort_track` ≥8 epoch marks; cohort track not active.\n\n**graduated_allocation_track** (active): **HOLD** — only 2 lifetime equity marks, 4 trades (`learning_tracks_review.reviews.graduated_allocation.metrics`); `revisit_when` needs ≥8 weekly marks.\n\n**capital_rotation_coordinator** (planned): **HOLD** — `exit_timing_cohorts` null in payload; near-miss archive ready (`exit_timing_near_miss.readiness.ready_for_probability_analysis` true) but live closed swap rotations below gate; `graduated_allocation` rebalance_log too thin.\n\n**conviction_weighted_sizing** (planned): **HOLD** — upstream dependencies unmet.\n\n**cross_shard_winner_selection** (planned): **HOLD** — `convergence_summary.cross_shard_phase_status` planned; no second shard at Phase 3.\n\n**hypothesis_first_exit** (active): **HOLD** — phase active but `hypothesis_outcomes` absent; observe via `exit_timing_near_miss` hold recovery 73.27% in `pre_buy_0.28_0.45` band until thesis linker populates.\n\n**super_director_auto_promote** (deferred): **RETIRE** (continue deferring) — `revisit_when` explicit human ops decision; `guardrails.human_promotion_required` true with primary underperforming market.","proposed_actions":"1. [monitoring] Human-ack forward pass on ai_judgment_exclusion_u4 vs live primary — expected learning value: first cost-aware week-pair read on whether u4 ladder replay alpha (`exclusion_ladder_replay.readiness.primary_return_delta_vs_actual` 0.057) converts to post-cost shadow differentiation; counts toward `filtered_cohort_track` ≥4-week gate.\n\n2. [monitoring] Primary epoch thickness alert (post-2026-08-26 reseed) — expected learning value: trigger when `ai_judgment.metrics.epoch.equity_marks` ≥2 and `benchmark_span_available` true so trajectory read on `min_conviction` 0.6 epoch is independent of lifetime −19.24% drag.\n\n3. [analysis] Ensure scoring experiment queued for hold->buy / signal_unchanged transition keys — expected learning value: convert `analysis_review.sections.proposed_experiments` item 1 (offline conviction-timing overlay; 1w hit_rate 0.406) from review prose into a tracked `analysis_tasks` entry before next analysis-review cycle.\n\n4. [paper_churn] Human-ack hold-vs-swap probability strand on near-miss archive — expected learning value: stratify `exit_timing_near_miss` 202 hold-closed (73.27% recovery in `pre_buy_0.28_0.45`) vs 209 swap-closed (118/209 replacement_outperformed) to inform exit policy without crude mark stops; aligns with `analysis_review.sections.proposed_experiments` item 5 and `paper_learning_review.sections.proposed_experiments` item 3.\n\n5. [offline_sim] Continue ldr-20260823-03 archive-lab full-period rebalance replay — expected learning value: reconcile +2.03% archived buy-tier 7d excess with −19.24% primary paper excess by simulating costs on pre-logging history (`experiment_assessment.by_status.continue`).","horizon_fragments":"- [timing_lag,trajectory_evidence,hypothesis] 1w directional hit_rate is 0.406 but 94.48% of labeled events realize within 4w with median 1 week — what if conviction/timing overlays explicitly delay actionable upgrades by 1–2 screens rather than down-weighting magnitude?\n\n- [exclusion_step_divergence,u3_vs_u4,hypothesis] Archive optimizer selects u3 (alpha 0.00261) while replay/shadow infrastructure targets u4 (alpha 0.000508) — what if the bettable floor is step-dependent on replay return_delta rather than cumulative archive alpha alone?","defer":"- **filtered_cohort_track** (`vision.phases[2]`): equal-weight cohort paper deployment — wait until u4 shadow accumulates ≥4 consecutive forward week-pairs with differentiation and human ack; exclusion shadow exists but forward history insufficient.\n\n- **regime_slices_8_16_24** (`vision.phases[1]`): rolling 8/16/24 decay panels — wait until `history_run_count` ≥16 and exclusion archive ≥12 consecutive week pairs (currently 11 runs, 9 pairs).\n\n- **loser_pattern_lab / filter_invention_loop / dual_objective_calibration** (`vision.phases[3–5]`): blocked by upstream phases and insufficient history.\n\n- Auto-apply `knob_calibration_priors` (`regime_summary.knob_calibration_ready` false; priors recommend `min_conviction` 0.0 / `max_positions` 5 with low confidence) — conflicts with live 0.6 floor and `convergence_summary.tensions` calibration direction conflicts.\n\n- Live assign_signal / base-screen mutation from trajectory transition keys — defer until offline scoring experiment confirms 4w hit_rate lift above 0.519 (`analysis_review.sections.defer`).\n\n- **capital_rotation_coordinator / conviction_weighted_sizing / cross_shard_winner_selection**: dependencies on graduated_allocation epoch marks, exit-timing live cohorts, and second-shard Phase 3 not met.\n\n- **hypothesis_first_exit thesis rotation** — defer until `hypothesis_outcomes.readiness.ready_for_thesis_outcome_analysis` true in a future payload (`hypothesis_integrity` currently null).\n\n- **super_director_auto_promote** (`vision.phases[11]`): explicit human ops decision only; no auto-promote to engineering."},"fragments_compiled":{"compiled_at":"2026-08-30T15:08:56.605985+00:00","run_stamp":"20260830","created_count":2,"skipped_duplicates":[],"fragments":[{"id":"frag-20260830-01","text":"1w directional hit_rate is 0.406 but 94.48% of labeled events realize within 4w with median 1 week — what if conviction/timing overlays explicitly delay actionable upgrades by 1–2 screens rather than down-weighting magnitude?","tags":["timing_lag","trajectory_evidence","hypothesis"],"status":"open","source":"learning_director:20260830","added_at":"2026-08-30T15:08:56+00:00"},{"id":"frag-20260830-02","text":"Archive optimizer selects u3 (alpha 0.00261) while replay/shadow infrastructure targets u4 (alpha 0.000508) — what if the bettable floor is step-dependent on replay return_delta rather than cumulative archive alpha alone?","tags":["exclusion_step_divergence","u3_vs_u4","hypothesis"],"status":"open","source":"learning_director:20260830","added_at":"2026-08-30T15:08:56+00:00"}]}}