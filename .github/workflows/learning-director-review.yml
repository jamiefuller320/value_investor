name: FTSE Learning Director Review

# Observe-only weekly orchestration synthesis (regime, convergence, vision roadmap).
# Disable via docs/data/paper_automation/review_policy.json before live capital.
on:
  schedule:
    - cron: "55 10 * * 0"
  workflow_dispatch:
    inputs:
      force:
        description: "Run even if learning director review already succeeded today"
        type: boolean
        default: false

permissions:
  contents: write
  actions: read

concurrency:
  group: ftse-learning-director-review
  cancel-in-progress: false

jobs:
  check:
    runs-on: ubuntu-latest
    outputs:
      should_run: ${{ steps.gate.outputs.should_run }}
    steps:
      - name: Skip if learning director review already ran today
        id: gate
        uses: actions/github-script@v8
        env:
          FORCE: ${{ github.event.inputs.force || 'false' }}
        with:
          script: |
            const force = process.env.FORCE === 'true';
            if (force) {
              core.setOutput('should_run', 'true');
              return;
            }
            const owner = context.repo.owner;
            const repo = context.repo.repo;
            const workflow_id = 'learning-director-review.yml';
            const runId = context.runId;
            const dayStart = new Date(Date.UTC(
              new Date().getUTCFullYear(),
              new Date().getUTCMonth(),
              new Date().getUTCDate(),
            )).toISOString();
            const { data } = await github.rest.actions.listWorkflowRuns({
              owner, repo, workflow_id, per_page: 20,
            });
            const prior = (data.workflow_runs || []).filter((r) =>
              r.id !== runId
              && r.created_at >= dayStart
              && (r.event === 'workflow_dispatch' || r.event === 'schedule')
            );
            const succeeded = prior.find((r) => r.conclusion === 'success');
            if (succeeded) {
              core.info(`Skipping — learning director review already succeeded today (run ${succeeded.id})`);
              core.setOutput('should_run', 'false');
              return;
            }
            core.setOutput('should_run', 'true');

  learning-director-review:
    needs: check
    if: needs.check.outputs.should_run == 'true'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5

      - uses: actions/setup-python@v6
        with:
          python-version: "3.12"

      - name: Install package
        run: pip install -e .

      - name: Check enable flag and inputs
        id: ready
        run: |
          set +e
          ftse-learning-director status --json > /tmp/ldr_status.json
          cat /tmp/ldr_status.json
          enabled=$(python3 - <<'PY'
          import json
          print('true' if json.load(open('/tmp/ldr_status.json')).get('enabled') else 'false')
          PY
          )
          if [ "$enabled" != "true" ]; then
            echo "skip=true" >> "$GITHUB_OUTPUT"
            echo "Learning director disabled — skipping agent run"
            exit 0
          fi
          ftse-learning-director payload --json > /tmp/ldr_payload.json
          code=$?
          cat /tmp/ldr_payload.json
          if [ "$code" -ne 0 ]; then
            echo "skip=true" >> "$GITHUB_OUTPUT"
            echo "Learning director inputs not ready — skipping agent run"
            exit 0
          fi
          echo "skip=false" >> "$GITHUB_OUTPUT"

      - name: Run learning director review
        if: steps.ready.outputs.skip != 'true'
        env:
          CURSOR_API_KEY: ${{ secrets.CURSOR_API_KEY }}
        run: ftse-learning-director run --json

      - name: Sync main before commit
        if: steps.ready.outputs.skip != 'true'
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git pull --rebase --autostash origin main

      - name: Commit learning director review artifacts
        if: steps.ready.outputs.skip != 'true'
        uses: stefanzweifel/git-auto-commit-action@v6
        with:
          commit_message: "chore: learning director weekly review [skip ci]"
          file_pattern: |
            docs/data/learning_director_review.json
            docs/data/learning_director_review.md
            docs/data/learning_director_tasks.json
          skip_fetch: false
